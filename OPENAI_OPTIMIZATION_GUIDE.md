# 🚀 Руководство по оптимизации OpenAI API

## Проблема
Бот работает медленно из-за долгих ответов от GPT-5 (20-30 секунд на запрос).

## Решения

### 1. Использование быстрых моделей

#### GPT-4o-mini (Рекомендуется для скорости)
- **Время ответа**: ~3 секунды
- **Качество**: Хорошее
- **Стоимость**: $0.00015 за 1K входных токенов
- **Применение**: Простые задачи, быстрые ответы

#### GPT-4o (Сбалансированный выбор)
- **Время ответа**: ~8 секунд
- **Качество**: Высокое
- **Стоимость**: $0.005 за 1K входных токенов
- **Применение**: Стандартные задачи

#### GPT-5 (Только для сложных задач)
- **Время ответа**: ~25 секунд
- **Качество**: Высшее
- **Стоимость**: $0.005 за 1K входных токенов
- **Применение**: Сложная логика, требующая высокого качества

### 2. Настройки окружения

```bash
# Использовать быструю модель по умолчанию
USE_FAST_MODEL_BY_DEFAULT=true

# Быстрая модель
OPENAI_FAST_MODEL=gpt-4o-mini

# Сбалансированная модель
OPENAI_BALANCED_MODEL=gpt-4o

# Качественная модель
OPENAI_QUALITY_MODEL=gpt-5

# Ограничение итераций
OPENAI_MAX_TOOL_CALLS=3

# Таймауты
OPENAI_REQUEST_TIMEOUT=30.0
OPENAI_CONNECT_TIMEOUT=10.0
```

### 3. Автоматический выбор модели

Бот автоматически выбирает модель в зависимости от сложности:

- **Простая задача** → GPT-4o-mini (3s)
- **Средняя задача** → GPT-4o (8s)
- **Сложная задача** → GPT-5 (25s)

### 4. Ожидаемые результаты

#### До оптимизации (GPT-5):
- Общее время: ~83 секунды
- OpenAI API: ~77 секунд
- Итерации: 4-5

#### После оптимизации (GPT-4o-mini):
- Общее время: ~15-20 секунд
- OpenAI API: ~9-12 секунд
- Итерации: 2-3

### 5. Мониторинг производительности

В логах теперь отображается:
```
🚀 Используем быструю модель по умолчанию: gpt-4o-mini
📊 Модель: gpt-4o-mini | Скорость: fast | Ожидаемое время: 3.0s | Рекомендация: Рекомендуется для скорости
⏱️ Общее время выполнения: 12.45 секунд
📊 Модель gpt-4o-mini показала быструю производительность
```

### 6. Переключение моделей

#### Для максимальной скорости:
```bash
USE_FAST_MODEL_BY_DEFAULT=true
OPENAI_FAST_MODEL=gpt-4o-mini
```

#### Для баланса скорости и качества:
```bash
USE_FAST_MODEL_BY_DEFAULT=true
OPENAI_FAST_MODEL=gpt-4o
```

#### Для максимального качества (медленно):
```bash
USE_FAST_MODEL_BY_DEFAULT=false
OPENAI_DEFAULT_MODEL=gpt-5
```

### 7. Рекомендации

1. **Начните с GPT-4o-mini** для тестирования скорости
2. **Если качество недостаточно** → переключитесь на GPT-4o
3. **GPT-5 используйте только** для сложных аналитических задач
4. **Мониторьте логи** для отслеживания производительности
5. **Настройте таймауты** под ваши требования

### 8. Стоимость

#### GPT-4o-mini (самый дешевый):
- 1000 входных токенов = $0.00015
- 1000 выходных токенов = $0.0006

#### GPT-4o:
- 1000 входных токенов = $0.005
- 1000 выходных токенов = $0.015

#### GPT-5:
- 1000 входных токенов = $0.005
- 1000 выходных токенов = $0.015

**Вывод**: GPT-4o-mini в 33 раза дешевле GPT-4o/GPT-5 для входных токенов!
