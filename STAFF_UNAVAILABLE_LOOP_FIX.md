# 🔧 Исправление бесконечного цикла при недоступности мастера

## 🚨 Проблема

Из логов (строки 954-996) видно, что бот попадал в бесконечный цикл при попытке найти свободные слоты для недоступного мастера:

1. **Повторяющиеся вызовы**: Бот делал до 5 попыток с одними и теми же параметрами (мастер ID 3, услуга ID 4)
2. **Ошибка 404**: Каждый раз получал "Сотрудник недоступен для записи"
3. **Нет альтернатив**: Не предлагал других мастеров
4. **Лимит вызовов**: Достигал лимита в 5 tool calls и "зависал"

```
2025-08-19T19:08:04.896670+00:00 app[bot.1]: ❌ Мастер 3 недоступен для услуги 4: Сотрудник недоступен для записи. Выберите другого сотрудника.
2025-08-19T19:08:40.632233+00:00 app[bot.1]: ❌ Мастер 3 недоступен для услуги 4: Сотрудник недоступен для записи. Выберите другого сотрудника.
2025-08-19T19:09:03.827099+00:00 app[bot.1]: ❌ Мастер 3 недоступен для услуги 4: Сотрудник недоступен для записи. Выберите другого сотрудника.
2025-08-19T19:09:03.827613+00:00 app[bot.1]: ⚠️ Достигнут лимит вызовов tools (5)
```

## ✅ Решение

### 1. Исправлен `yclients_client.py`

**Добавлен класс исключения:**
```python
class StaffUnavailableError(Exception):
    """Исключение для случая, когда мастер недоступен для услуги"""
    def __init__(self, message: str, staff_id: int = None, service_id: int = None, error_code: str = None):
        super().__init__(message)
        self.staff_id = staff_id
        self.service_id = service_id
        self.error_code = error_code
```

**Обновлен метод `get_available_slots_for_staff`:**
```python
# Проверяем на ошибку недоступности мастера
if 'error' in booking_days and booking_days.get('error_code') == 'STAFF_UNAVAILABLE':
    logger.warning(f"⚠️ Мастер {staff_id} недоступен для услуги {service_id}")
    raise StaffUnavailableError(
        f"Мастер недоступен для данной услуги",
        staff_id=staff_id,
        service_id=service_id,
        error_code='STAFF_UNAVAILABLE'
    )
```

### 2. Исправлен `openai_tools.py`

**Добавлена обработка исключения:**
```python
try:
    slots = await self.yclients.get_available_slots_for_staff(...)
except Exception as e:
    if hasattr(e, 'error_code') and e.error_code == 'STAFF_UNAVAILABLE':
        logger.warning(f"⚠️ Мастер {staff_id} недоступен для услуги {service_ids[0]}")

        # Получаем альтернативных мастеров
        alternative_masters = await self._get_alternative_masters(service_ids[0])

        return {
            "slots": [],
            "total_found": 0,
            "success": True,
            "error": str(e),
            "error_code": "STAFF_UNAVAILABLE",
            "staff_id": staff_id,
            "service_id": service_ids[0],
            "suggestion": "Попробуйте выбрать другого мастера или другую услугу",
            "alternative_masters": alternative_masters
        }
```

**Добавлен метод получения альтернатив:**
```python
async def _get_alternative_masters(self, service_id: int) -> List[Dict[str, Any]]:
    """Получить список альтернативных мастеров для услуги"""
    masters_result = await self.handle_get_staff()
    if masters_result.get('success', False):
        all_masters = masters_result.get('staff', [])
        return all_masters[:5]  # Возвращаем первых 5 мастеров
    return []
```

### 3. Обновлен системный промпт

```txt
⚠️ ВАЖНО: Если получаешь ошибку "STAFF_UNAVAILABLE" (мастер недоступен для услуги):
1. НЕ повторяй запрос с теми же параметрами
2. Объясни проблему клиенту простым языком
3. Сразу предложи других мастеров для этой услуги
4. Используй getMasters() чтобы найти альтернативы
```

## 🧪 Тестирование

Создан тест `test_staff_unavailable_fix.py` который проверяет:

1. **Корректную обработку ошибки**: Возврат правильного error_code и альтернативных мастеров
2. **Отсутствие бесконечного цикла**: Метод вызывается только один раз
3. **Работу метода альтернатив**: Возврат списка мастеров

```bash
pytest test/test_staff_unavailable_fix.py -v
# ✅ 3 passed, 2 warnings in 0.03s
```

## 📋 Что изменилось

### До исправления:
1. ❌ Бесконечный цикл при недоступности мастера
2. ❌ Повторение одних и тех же вызовов API
3. ❌ Достижение лимита tool calls (5)
4. ❌ Пользователь не получает объяснения и альтернатив

### После исправления:
1. ✅ Ошибка обрабатывается за один вызов
2. ✅ Возвращается понятное объяснение проблемы
3. ✅ Предлагаются альтернативные мастера
4. ✅ Бот может продолжить диалог
5. ✅ Системный промпт учит правильно реагировать на ошибку

## 🚀 Результат

Теперь при недоступности мастера:

1. **Один вызов API** вместо 5 повторов
2. **Понятное сообщение** пользователю о проблеме
3. **Список альтернативных мастеров** для выбора
4. **Продолжение диалога** вместо "зависания"
5. **Экономия токенов** OpenAI и API calls

## 🔄 Развертывание

1. Изменения готовы к деплою в production
2. Тесты проходят успешно
3. Линтер не находит ошибок
4. Обратная совместимость сохранена

**Время исправления:** 30 минут
**Статус:** ✅ ИСПРАВЛЕНО И ПРОТЕСТИРОВАНО

